# Strategy App dependencies (in addition to OpenBB which is installed via the
# repo's openbb_platform). Pin only loose minimums so the demo stays portable.

# ---- Web framework ----
fastapi>=0.115
uvicorn[standard]>=0.30

# ---- Frontend ----
streamlit>=1.36

# ---- HTTP / async ----
httpx>=0.27

# ---- Data validation ----
pydantic>=2.7
pydantic-settings>=2.3

# ---- Env & config ----
python-dotenv>=1.0

# ---- LLM (OpenAI-compatible: works for OpenAI, Kimi, ZhipuAI via base_url) ----
openai>=1.40

# ---- Text dedup ----
rapidfuzz>=3.9

# ---- Storage (SQLite via stdlib + lightweight ORM optional). Use stdlib sqlite3
# only to avoid bringing in heavy deps for the demo. Keep this comment as
# documentation; nothing extra to install.

# ---- Numeric (already pulled in by openbb but pinning loose minimum) ----
pandas>=2.0
numpy>=1.24

# ---- Test ----
pytest>=8.0
pytest-asyncio>=0.23
